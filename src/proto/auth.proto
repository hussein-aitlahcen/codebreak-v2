syntax = "proto3";

import "shared.proto";

package CodebreakV2.Shared.Intercom;

service AuthService {
    rpc AccountBan (BanRequest) returns (BanResponse);
    rpc WorldRegister (WorldRegisterRequest) returns (WorldRegisterResponse);
}